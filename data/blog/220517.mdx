---
title: 220517 Today I Learned
date: "2022-05-17"
tags:
  [
    "daily",
    "study",
    "javascript",
    "apollo",
    "graphql",
    "instaclone",
    "nomadcoders",
    "speak",
    "hbr",
    "wsj",
  ]
draft: false
summary:
images: []
layout: PostLayout
---

<br />

# Today Plan

- [x] Todopod Front - login
- [x] HBR / WSJ Study
- [x] DeepDive JS # 44 Rest API
- [x] Figma 기본 구성 익히기
- [x] InstaClone # 14.3 ~
- [x] Speak 2 Chapter

<br />

# Figma

- `연정’s Figma` 유튜브 참고
- 버튼을 만들어보며 `Auto Layout` 기능을 시험해봄
- `Local Style` 에 대해서 배움

# ToDoPod front login page

- Login Page 만들기 스타트
- CRA !
- 각종 패키지 설치 중
- codegen 어떻게 설치했더라…
- npx graphql-codegen 해서 세팅하면 됨
- codegen 세팅 완료
- 이제 login 페이지를 위한 dom rounter 세팅 !
- Apollo makeVar 을 이용해 isLoggedIn 변수 만들어서 route 에서 사용하기
- Styled-components Theme 의 타입을 정의하고 GlobalStyles도 정의한다.
- styles.ts 에 theme과 global style을 정의하고 App.tsx 에 ThemeProvider 컴포넌트와 GlobalStyle 을 호출한다
- Google Font 에서 Open Sans 적용 완료
- React Helmet PageTitle 컴포넌트 완료
- React Helmet 을 쓸 때는 꼭 Root 파일에 HelmetProvider를 Wrapping 해야함
- login form validation 을 함
- Layout / form / button Component 분리
- Login Mutation 구현
- Apollo Login Variable 구현
- DarkMode / LightMode Toggle Function 구현

# InstaClone Native

- styled-components/native
- Appearance from react-native (apearance 별도 패키지는 depraciated 됨)

```tsx
const themeSubscription = Appearance.addChangeListener(({ colorScheme }) => {
	console.log(colorScheme);
});
```

- 이렇게 Apearnace 를 이용해 theme 변경을 체크하여 앱 테마를 변경할 수 있다.
- 강의에서는 ApearanceProvider를 사용했는데 새로 변경된 버전에서는 어떻게 사용하는지 잘 모르겠다.

## 14.4 Welcome Page

- Container flex: 1 (Native 에서만 허용되는 것으로 페이지 전체를 다 사용함)
- Image styled-components/native 는 height 까지 설정해줘야함
- TouchableOpacity 컴포넌트는 onPress 이벤트 리스너가 있음
- Welcome 컴포넌트에 prop 으로 받아놓은 navigation 객체를 이용해 navigate 메서드로 CreateAccount 나 Login 페이지로 이동하도록
- 각 버튼의 onPress 이벤트에 핸들러로 등록함
- React Native 는 기본적으로 flex-direction:column 이 적용되어 있으므로 align-items 나 justify-content 만 정의해주면 됨
- styled-components/native 에서는 부모 CSS 가 자식에게 상속되지 않아 따로따로 지정해줘야함
- onPress type 정의

```tsx
type Props {
   onPress: (event: GestureResponderEvent) => void
}
type Props {
   onPress(): void
}
type Props {
   onPress(params: type): void
}
-
```

## 14.5 Create Account part One

- Navigator 에서 options 와 screenOptions 를 이용해 header 부분 형태를 바꿀 수 있음

## 14.6 Create Account part Two

- Create Account TextInput 으로 Form 만들기
- returnKeyType 등을 이용하여 키보드 구성 바꾸기
- secureTextEntry 비밀번호 부분에는 이 옵션을 꼭 넣어줘야함
- Web 에서는 Image 요소가 안나오는데 이유를 모르겠음 -> 해결 : max-widght 나 max-height 를 쓰지 말고 width/height 로 정확하게 잡아줘야함

## 14.7 Natural Form UseRef

- 키보드가 input 을 가리는 문제 해결하기
- ref : 가리키는 것
- typescript 에서 useRef 와 onChange Event 사용하는 방법
  https://velog.io/@jjbrother/Reactnative-Typescript%EC%97%90%EC%84%9C-TextInput-onChange-%EA%B3%BC-useRef
- Platform.OS 을 이용하면 현재 어떤 OS 에서 앱이 작동중인지 확인할 수 있다.

## 14.8 Natural Form

- KeyboardAvoidingView 컴포넌트는 키보드에 가려지는걸 방지함 behavior 옵션으로 padding 을 누르면 다른 요소도 함께 움직일 수 있음
- https://reactnative.dev/docs/keyboardavoidingview
- Layout 컴포넌트에서 `TouchableWithoutFeedback` 과 `Keyboard.dismiss()` 를 이용하여 키보드 사용 중에 배경을 누르면 키보드가 사라지도록 한다.
- https://reactnative.dev/docs/touchablewithoutfeedback
- https://reactnative.dev/docs/keyboard

## 14.9 Login Screen

- Login page 를 만들며 KeyboardAvoidingView 를 AuthLayout 으로 옮겨준다.

## 14.10 React Hooks Forms on Native

- TouchableWithoutFeedback 때문에 web 에서는 키보드가 적상 작동하지 않는 문제가 있다.

```tsx
  disabled={Platform.OS === "web"}
```

- 이 방법으로 해결했다.
- Layout 에서 max-width 를 정해주지 않으니 web 에서는 가로가 폼으로 꽉 차버리고 만다
- 이를 해결하기 위해 Wrapper View 를 새로 만들어 Container 안쪽으로 감싸고 max-width 를 400px 로 지정해준다
- Container 에 max-width 를 지정해버리면 제일 외곽 배경이 비어버린다!
- onNext 함수가 중복 사용되기 때문에 authShared 파일로 옮겨서 관리한다.

## 14.10 React Hook Form

- npm i react-hook-form

# NewsPresso HBR Vol.1.3

## 4 Ways to Create A Learning Culture on Your Team.

- `implication`: 영향 (결과)
- What you know is less relevant than what you may learn, and knowing the answer to questions is less critical than having the ability to ask the right questions in the first place.
- Mine : 너가 지금 알고 있는 것은 앞으로 배울 것보다 덜 의의가 있으며 질문에 대한 답을 아는 것은 최초로 올바른 질문을 할 수 있는 능력보다 덜 중요하다.
- `relevant` : (논의 중인 주제·생각하는 상황과 밀접하게) 관련 있는, 적절한 / (사람들의 삶 등에) 의의가 있는 (유의미한)
- `norm` : 표준
- It is impossible to trigger deliberate changes in your team's or organization's culture unless you actually put in place formal reward systems to entice them.
- Mine : 만약 당신이 실제로 공식적인 보상을 시행함으로써 팀원들을 유도하지 않는다면 팀원과 조직 문화의 자발적인 변화를 촉발시키기는 어려울 것이다.
- Book's : 실제로 그들을 유도할 수 있는 공식적인 보상 시스템을 준비하지 않았다면, 당신의 팀이나 조직에서의 계획적 변화를 시작하는 것은 불가능하다.
- `deliberate` : 고의의, 의도적인 (계획적인)
- `put in place` : 시행하다
- `entice` : 유도하다
- it's also creating a climate that nurtures critical thinking, where challenging authority and speaking up are encouraged, even if it means creating discord.
- Mine : 이것은 또한 비록 불화를 일으키는 것을 의미할지라도 권위에 도전하고 목소리를 높이는 것을 장려하는 분위기를 만드는 것이다.
- Book's : 그것은 불화가 일어날지라도 권한에 대한 도전과 자신의 의견을 거리낌 없이 말할 수 있는 분위기를 만들어 내는 것에 대한 것이다.
- `nurture` : (생각 등을) 키우다.
- `discord` : 불화, 다툼
- `intervention` : 조정, 중재
- `substitute` : 대신하다. 대치되다. 교체하다
- `euphemism` : 완곡 어구 (표현)
- And the more senior that leaders are, the more impactful their behaviors will be on the rest of the organization.
- Mine : 리더가 더 연장자일수록, 그들의 행동이 조직 나머지들에게 더 강한 영향을 끼칠 것이다.
- Book's : 리더들의 경력이 많을수록 그들의 행동은 나머지 조직에 더욱 큰 영향을 줄 것이다.
- `preach` : 설교하다
- `imperative` : 반드시 해야하는 긴요한 / 위엄 있는 / 명령법
- `undermine` : (자신감, 권위 등을) 약화시키다, 기반을 약화시키다.
- `augment` : 늘리다, 증가시키다.
- `propensity` : 경향 (성향)
